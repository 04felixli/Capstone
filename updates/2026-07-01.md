# Session Update — 2026-07-01

## What was done

### Repository setup
- Initialized `CLAUDE.md` with setup commands, run commands, architecture overview, and key design decisions to preserve across sessions.
- Added `.gitattributes`: `* text=auto`, LF enforcement for `.py`, `.sh`, `.c/.h/.cpp`, and binary markers for model weights (`*.pt`) and all image/video formats used in testing.

### Documentation
- `docs/roadmap.md` — 7-phase development plan derived from the project spec, with checkboxes for each deliverable and spec IDs (F1–F7, N1–N5) mapped to acceptance tests. Includes a minimum viable scope fallback.
- `docs/research.md` — open research questions organized by phase, each ending with a concrete "Decision needed" so the file drives action rather than just noting options. Covers CV model selection and export, path boundary detection approaches, distance sensor hardware, sensor fusion strategy, haptic pattern design, and Pi hardware selection.

### File restructuring
Reorganized flat layout into a proper Python package:

```
haptos/
├── config.py
├── types.py          # replaces haptos_types.py — stdlib naming collision resolved
├── cv/
│   ├── camera.py
│   ├── detector.py
│   ├── postprocess.py
│   └── utils.py
├── sensor/           # Phase 3 placeholder
├── fusion/           # Phase 4 placeholder
└── feedback/         # Phase 5 placeholder
```

All imports updated to `haptos.*`. `types.py` and `haptos_types.py` (the old workaround) removed.

### Test suite (`feat/postprocess-tests` branch)
- `tests/cv/test_postprocess.py` — 23 pytest tests covering `map_bbox_to_region`, `filter_and_enrich_detections`, and `generate_navigation_hint`.
- `conftest.py` at root so pytest resolves `haptos.*` imports without needing a venv.
- `requirements-dev.txt` with `pytest>=8.0` kept separate from runtime deps.
- All 23 tests pass. No CV dependencies (cv2, ultralytics) required to run them.

## Next steps
- Merge `feat/postprocess-tests` into `main`.
- Write `scripts/benchmark.py` to measure per-frame latency end-to-end (target: p90 < 300 ms per spec F4).
- Begin research decisions in `docs/research.md` — path boundary detection approach and Pi model export format are the two that unblock the most subsequent work.
